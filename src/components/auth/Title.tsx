import styled from "styled-components";

const STitle = styled.h1`
  font-size: 40px;
  font-family: "Shadows Into Light", cursive;
`;

function Title() {
  return <STitle>NomadCoffee</STitle>;
}

export default Title;
